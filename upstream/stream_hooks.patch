diff --git a/csrc/cpu/torch_bindings.cpp b/csrc/cpu/torch_bindings.cpp
index d011ff038..352ddfc59 100644
--- a/csrc/cpu/torch_bindings.cpp
+++ b/csrc/cpu/torch_bindings.cpp
@@ -8,6 +8,14 @@
 // libraries use different ISAs.
 #define TORCH_EXTENSION_NAME _C
 
+extern "C" __attribute__((noinline)) void stream_start_hook() {}
+extern "C" __attribute__((noinline)) void stream_emit_hook() {}
+extern "C" __attribute__((noinline)) void stream_end_hook() {}
+
+static void stream_start_hook_op(const torch::Tensor&) { stream_start_hook(); }
+static void stream_emit_hook_op(const torch::Tensor&) { stream_emit_hook(); }
+static void stream_end_hook_op(const torch::Tensor&) { stream_end_hook(); }
+
 std::string init_cpu_threads_env(const std::string& cpu_ids);
 
 void release_dnnl_matmul_handler(int64_t handler);
@@ -128,6 +136,14 @@ void cpu_fused_moe(torch::Tensor& output, const torch::Tensor& input,
 
 TORCH_LIBRARY_EXPAND(TORCH_EXTENSION_NAME, ops) {
   // vLLM custom ops
+  ops.def("stream_start_hook(Tensor dummy) -> ()");
+  ops.impl("stream_start_hook", torch::kCPU, &stream_start_hook_op);
+
+  ops.def("stream_emit_hook(Tensor dummy) -> ()");
+  ops.impl("stream_emit_hook", torch::kCPU, &stream_emit_hook_op);
+
+  ops.def("stream_end_hook(Tensor dummy) -> ()");
+  ops.impl("stream_end_hook", torch::kCPU, &stream_end_hook_op);
 
   ops.def(
       "dynamic_4bit_int_moe("
diff --git a/csrc/torch_bindings.cpp b/csrc/torch_bindings.cpp
index d98e987d9..47913ea82 100644
--- a/csrc/torch_bindings.cpp
+++ b/csrc/torch_bindings.cpp
@@ -6,6 +6,14 @@
 #include <torch/library.h>
 #include <torch/version.h>
 
+extern "C" __attribute__((noinline)) void stream_start_hook() {}
+extern "C" __attribute__((noinline)) void stream_emit_hook() {}
+extern "C" __attribute__((noinline)) void stream_end_hook() {}
+
+static void stream_start_hook_op(const torch::Tensor&) { stream_start_hook(); }
+static void stream_emit_hook_op(const torch::Tensor&) { stream_emit_hook(); }
+static void stream_end_hook_op(const torch::Tensor&) { stream_end_hook(); }
+
 // Note on op signatures:
 // The X_meta signatures are for the meta functions corresponding to op X.
 // They must be kept in sync with the signature for X. Generally, only
@@ -19,6 +27,14 @@
 TORCH_LIBRARY_EXPAND(TORCH_EXTENSION_NAME, ops) {
   // vLLM custom ops
   //
+  ops.def("stream_start_hook(Tensor dummy) -> ()");
+  ops.impl("stream_start_hook", torch::kCPU, &stream_start_hook_op);
+
+  ops.def("stream_emit_hook(Tensor dummy) -> ()");
+  ops.impl("stream_emit_hook", torch::kCPU, &stream_emit_hook_op);
+
+  ops.def("stream_end_hook(Tensor dummy) -> ()");
+  ops.impl("stream_end_hook", torch::kCPU, &stream_end_hook_op);
 
   ops.def(
       "persistent_masked_m_silu_mul_quant(Tensor input, Tensor counts, Tensor! "
diff --git a/vllm/entrypoints/anthropic/serving.py b/vllm/entrypoints/anthropic/serving.py
index f301ed499..ecfa34b12 100644
--- a/vllm/entrypoints/anthropic/serving.py
+++ b/vllm/entrypoints/anthropic/serving.py
@@ -42,6 +42,11 @@ from vllm.entrypoints.openai.engine.protocol import (
     StreamOptions,
 )
 from vllm.entrypoints.openai.models.serving import OpenAIServingModels
+from vllm.entrypoints.utils import (
+    stream_emit_hook,
+    stream_end_hook,
+    stream_start_hook,
+)
 
 if TYPE_CHECKING:
     from vllm.entrypoints.serve.render.serving import OpenAIServingRender
@@ -479,6 +484,7 @@ class AnthropicServingMessages(OpenAIServingChat):
         generator: AsyncGenerator[str, None],
     ) -> AsyncGenerator[str, None]:
         try:
+            stream_start_hook()
 
             class _ActiveBlockState:
                 def __init__(self) -> None:
@@ -559,6 +565,10 @@ class AnthropicServingMessages(OpenAIServingChat):
                 state.start(block)
                 return event
 
+            def _emit(event: str) -> str:
+                stream_emit_hook()
+                return event
+
             async for item in generator:
                 if item.startswith("data:"):
                     data_str = item[5:].strip().rstrip("\n")
@@ -569,8 +579,9 @@ class AnthropicServingMessages(OpenAIServingChat):
                         data = stop_message.model_dump_json(
                             exclude_unset=True, exclude_none=True
                         )
-                        yield wrap_data_with_event(data, "message_stop")
-                        yield "data: [DONE]\n\n"
+                        yield _emit(wrap_data_with_event(data, "message_stop"))
+                        stream_end_hook()
+                        yield _emit("data: [DONE]\n\n")
                     else:
                         origin_chunk = ChatCompletionStreamResponse.model_validate_json(
                             data_str
@@ -595,13 +606,13 @@ class AnthropicServingMessages(OpenAIServingChat):
                             )
                             first_item = False
                             data = chunk.model_dump_json(exclude_unset=True)
-                            yield wrap_data_with_event(data, "message_start")
+                            yield _emit(wrap_data_with_event(data, "message_start"))
                             continue
 
                         # last chunk including usage info
                         if len(origin_chunk.choices) == 0:
                             for event in stop_active_block():
-                                yield event
+                                yield _emit(event)
                             stop_reason = self.stop_reason_map.get(
                                 finish_reason or "stop"
                             )
@@ -618,7 +629,7 @@ class AnthropicServingMessages(OpenAIServingChat):
                                 ),
                             )
                             data = chunk.model_dump_json(exclude_unset=True)
-                            yield wrap_data_with_event(data, "message_delta")
+                            yield _emit(wrap_data_with_event(data, "message_delta"))
                             continue
 
                         if origin_chunk.choices[0].finish_reason is not None:
@@ -633,13 +644,13 @@ class AnthropicServingMessages(OpenAIServingChat):
                             else:
                                 if state.block_type != "thinking":
                                     for event in stop_active_block():
-                                        yield event
+                                        yield _emit(event)
                                     start_event = start_block(
                                         AnthropicContentBlock(
                                             type="thinking", thinking=""
                                         )
                                     )
-                                    yield start_event
+                                    yield _emit(start_event)
                                 chunk = AnthropicStreamEvent(
                                     index=(
                                         state.block_index
@@ -653,7 +664,9 @@ class AnthropicServingMessages(OpenAIServingChat):
                                     ),
                                 )
                                 data = chunk.model_dump_json(exclude_unset=True)
-                                yield wrap_data_with_event(data, "content_block_delta")
+                                yield _emit(
+                                    wrap_data_with_event(data, "content_block_delta")
+                                )
 
                         if origin_chunk.choices[0].delta.content is not None:
                             if origin_chunk.choices[0].delta.content == "":
@@ -661,11 +674,11 @@ class AnthropicServingMessages(OpenAIServingChat):
                             else:
                                 if state.block_type != "text":
                                     for event in stop_active_block():
-                                        yield event
+                                        yield _emit(event)
                                     start_event = start_block(
                                         AnthropicContentBlock(type="text", text="")
                                     )
-                                    yield start_event
+                                    yield _emit(start_event)
                                 chunk = AnthropicStreamEvent(
                                     index=(
                                         state.block_index
@@ -679,7 +692,9 @@ class AnthropicServingMessages(OpenAIServingChat):
                                     ),
                                 )
                                 data = chunk.model_dump_json(exclude_unset=True)
-                                yield wrap_data_with_event(data, "content_block_delta")
+                                yield _emit(
+                                    wrap_data_with_event(data, "content_block_delta")
+                                )
 
                         # tool calls - process all tool calls in the delta
                         if len(origin_chunk.choices[0].delta.tool_calls) > 0:
@@ -699,7 +714,7 @@ class AnthropicServingMessages(OpenAIServingChat):
                                         and tool_name is not None
                                     ):
                                         for event in stop_active_block():
-                                            yield event
+                                            yield _emit(event)
                                         start_event = start_block(
                                             AnthropicContentBlock(
                                                 type="tool_use",
@@ -708,7 +723,7 @@ class AnthropicServingMessages(OpenAIServingChat):
                                                 input={},
                                             )
                                         )
-                                        yield start_event
+                                        yield _emit(start_event)
                                     # Handle initial arguments if present
                                     if (
                                         tool_call.function
@@ -728,8 +743,10 @@ class AnthropicServingMessages(OpenAIServingChat):
                                             ),
                                         )
                                         data = chunk.model_dump_json(exclude_unset=True)
-                                        yield wrap_data_with_event(
-                                            data, "content_block_delta"
+                                        yield _emit(
+                                            wrap_data_with_event(
+                                                data, "content_block_delta"
+                                            )
                                         )
                                 else:
                                     # Incremental update - use index to find tool_use_id
@@ -753,7 +770,7 @@ class AnthropicServingMessages(OpenAIServingChat):
                                             ),
                                         )
                                         data = chunk.model_dump_json(exclude_unset=True)
-                                        yield wrap_data_with_event(
+                                        yield _emit(
                                             data, "content_block_delta"
                                         )
                             continue
@@ -766,8 +783,9 @@ class AnthropicServingMessages(OpenAIServingChat):
                         ),
                     )
                     data = error_response.model_dump_json(exclude_unset=True)
-                    yield wrap_data_with_event(data, "error")
-                    yield "data: [DONE]\n\n"
+                    yield _emit(wrap_data_with_event(data, "error"))
+                    stream_end_hook()
+                    yield _emit("data: [DONE]\n\n")
 
         except Exception as e:
             logger.exception("Error in message stream converter.")
@@ -776,8 +794,9 @@ class AnthropicServingMessages(OpenAIServingChat):
                 error=AnthropicError(type="internal_error", message=str(e)),
             )
             data = error_response.model_dump_json(exclude_unset=True)
-            yield wrap_data_with_event(data, "error")
-            yield "data: [DONE]\n\n"
+            yield _emit(wrap_data_with_event(data, "error"))
+            stream_end_hook()
+            yield _emit("data: [DONE]\n\n")
 
     async def count_tokens(
         self,
diff --git a/vllm/entrypoints/openai/chat_completion/serving.py b/vllm/entrypoints/openai/chat_completion/serving.py
index bf8beb9b9..46703747c 100644
--- a/vllm/entrypoints/openai/chat_completion/serving.py
+++ b/vllm/entrypoints/openai/chat_completion/serving.py
@@ -60,7 +60,13 @@ from vllm.entrypoints.openai.parser.harmony_utils import (
     parse_chat_output,
 )
 from vllm.entrypoints.openai.utils import maybe_filter_parallel_tool_calls
-from vllm.entrypoints.utils import get_max_tokens, should_include_usage
+from vllm.entrypoints.utils import (
+    get_max_tokens,
+    should_include_usage,
+    stream_emit_hook,
+    stream_end_hook,
+    stream_start_hook,
+)
 from vllm.inputs.data import ProcessorInputs
 from vllm.logger import init_logger
 from vllm.logprobs import Logprob
@@ -560,6 +566,7 @@ class OpenAIServingChat(OpenAIServing):
             all_previous_token_ids = None
 
         # Prepare the tool parser if it's needed
+        stream_start_hook()
         try:
             if tool_choice_auto and self.tool_parser:
                 if tokenizer is None:
@@ -575,7 +582,9 @@ class OpenAIServingChat(OpenAIServing):
         except Exception as e:
             logger.exception("Error in tool parser creation.")
             data = self.create_streaming_error_response(e)
+            stream_emit_hook()
             yield f"data: {data}\n\n"
+            stream_end_hook()
             yield "data: [DONE]\n\n"
             return
 
@@ -636,6 +645,7 @@ class OpenAIServingChat(OpenAIServing):
                             )
 
                         data = chunk.model_dump_json(exclude_unset=True)
+                        stream_emit_hook()
                         yield f"data: {data}\n\n"
 
                     # Send response to echo the input portion of the
@@ -672,6 +682,7 @@ class OpenAIServingChat(OpenAIServing):
                                     )
 
                                 data = chunk.model_dump_json(exclude_unset=True)
+                                stream_emit_hook()
                                 yield f"data: {data}\n\n"
                     first_iteration = False
 
@@ -1203,6 +1214,7 @@ class OpenAIServingChat(OpenAIServing):
                         )
 
                     data = chunk.model_dump_json(exclude_unset=True)
+                    stream_emit_hook()
                     yield f"data: {data}\n\n"
 
             # once the final token is handled, if stream_options.include_usage
@@ -1230,6 +1242,7 @@ class OpenAIServingChat(OpenAIServing):
                 final_usage_data = final_usage_chunk.model_dump_json(
                     exclude_unset=True, exclude_none=True
                 )
+                stream_emit_hook()
                 yield f"data: {final_usage_data}\n\n"
 
             # report to FastAPI middleware aggregate usage across all choices
@@ -1259,12 +1272,15 @@ class OpenAIServingChat(OpenAIServing):
                     )
 
         except GenerationError as e:
+            stream_emit_hook()
             yield f"data: {self._convert_generation_error_to_streaming_response(e)}\n\n"
         except Exception as e:
             logger.exception("Error in chat completion stream generator.")
             data = self.create_streaming_error_response(e)
+            stream_emit_hook()
             yield f"data: {data}\n\n"
         # Send the final done message after all response.n are finished
+        stream_end_hook()
         yield "data: [DONE]\n\n"
 
     async def chat_completion_full_generator(
diff --git a/vllm/entrypoints/openai/completion/serving.py b/vllm/entrypoints/openai/completion/serving.py
index 96cd7797c..c2000f61f 100644
--- a/vllm/entrypoints/openai/completion/serving.py
+++ b/vllm/entrypoints/openai/completion/serving.py
@@ -31,7 +31,13 @@ from vllm.entrypoints.openai.engine.serving import (
     clamp_prompt_logprobs,
 )
 from vllm.entrypoints.openai.models.serving import OpenAIServingModels
-from vllm.entrypoints.utils import get_max_tokens, should_include_usage
+from vllm.entrypoints.utils import (
+    get_max_tokens,
+    should_include_usage,
+    stream_emit_hook,
+    stream_end_hook,
+    stream_start_hook,
+)
 from vllm.exceptions import VLLMValidationError
 from vllm.inputs.data import ProcessorInputs
 from vllm.logger import init_logger
@@ -258,7 +264,10 @@ class OpenAIServingCompletion(OpenAIServing):
             response_json = response.model_dump_json()
 
             async def fake_stream_generator() -> AsyncGenerator[str, None]:
+                stream_start_hook()
+                stream_emit_hook()
                 yield f"data: {response_json}\n\n"
+                stream_end_hook()
                 yield "data: [DONE]\n\n"
 
             return fake_stream_generator()
@@ -291,6 +300,7 @@ class OpenAIServingCompletion(OpenAIServing):
         )
 
         try:
+            stream_start_hook()
             async for prompt_idx, res in result_generator:
                 prompt_token_ids = res.prompt_token_ids
                 prompt_logprobs = res.prompt_logprobs
@@ -413,6 +423,7 @@ class OpenAIServingCompletion(OpenAIServing):
                         )
 
                     response_json = chunk.model_dump_json(exclude_unset=False)
+                    stream_emit_hook()
                     yield f"data: {response_json}\n\n"
 
             total_prompt_tokens = sum(num_prompt_tokens)
@@ -439,17 +450,21 @@ class OpenAIServingCompletion(OpenAIServing):
                 final_usage_data = final_usage_chunk.model_dump_json(
                     exclude_unset=False, exclude_none=True
                 )
+                stream_emit_hook()
                 yield f"data: {final_usage_data}\n\n"
 
             # report to FastAPI middleware aggregate usage across all choices
             request_metadata.final_usage_info = final_usage_info
 
         except GenerationError as e:
+            stream_emit_hook()
             yield f"data: {self._convert_generation_error_to_streaming_response(e)}\n\n"
         except Exception as e:
             logger.exception("Error in completion stream generator.")
             data = self.create_streaming_error_response(e)
+            stream_emit_hook()
             yield f"data: {data}\n\n"
+        stream_end_hook()
         yield "data: [DONE]\n\n"
 
     def request_output_to_completion_response(
diff --git a/vllm/entrypoints/openai/responses/serving.py b/vllm/entrypoints/openai/responses/serving.py
index 6d0041813..8f2b018fd 100644
--- a/vllm/entrypoints/openai/responses/serving.py
+++ b/vllm/entrypoints/openai/responses/serving.py
@@ -105,7 +105,12 @@ from vllm.entrypoints.openai.responses.utils import (
     construct_tool_dicts,
     extract_tool_types,
 )
-from vllm.entrypoints.utils import get_max_tokens
+from vllm.entrypoints.utils import (
+    get_max_tokens,
+    stream_emit_hook,
+    stream_end_hook,
+    stream_start_hook,
+)
 from vllm.exceptions import VLLMValidationError
 from vllm.inputs.data import ProcessorInputs, token_inputs
 from vllm.logger import init_logger
@@ -1147,14 +1152,21 @@ class OpenAIServingResponses(OpenAIServing):
         start_index = 0 if starting_after is None else starting_after + 1
         current_index = start_index
 
+        stream_start_hook()
+
+        def _emit(event: StreamingResponsesResponse) -> StreamingResponsesResponse:
+            stream_emit_hook()
+            return event
+
         while True:
             new_event_signal.clear()
 
             # Yield existing events from start_index
             while current_index < len(event_deque):
                 event = event_deque[current_index]
-                yield event
+                yield _emit(event)
                 if getattr(event, "type", "unknown") == "response.completed":
+                    stream_end_hook()
                     return
                 current_index += 1
 
@@ -1857,7 +1869,12 @@ class OpenAIServingResponses(OpenAIServing):
             sequence_number += 1
             return event
 
+        def _emit(event: StreamingResponsesResponse) -> StreamingResponsesResponse:
+            stream_emit_hook()
+            return event
+
         async with AsyncExitStack() as exit_stack:
+            stream_start_hook()
             if self.use_harmony:
                 # TODO: in streaming, we noticed this bug:
                 # https://github.com/vllm-project/vllm/issues/25697
@@ -1876,18 +1893,22 @@ class OpenAIServingResponses(OpenAIServing):
                 status="in_progress",
                 usage=None,
             ).model_dump()
-            yield _increment_sequence_number_and_return(
-                ResponseCreatedEvent(
-                    type="response.created",
-                    sequence_number=-1,
-                    response=initial_response,
+            yield _emit(
+                _increment_sequence_number_and_return(
+                    ResponseCreatedEvent(
+                        type="response.created",
+                        sequence_number=-1,
+                        response=initial_response,
+                    )
                 )
             )
-            yield _increment_sequence_number_and_return(
-                ResponseInProgressEvent(
-                    type="response.in_progress",
-                    sequence_number=-1,
-                    response=initial_response,
+            yield _emit(
+                _increment_sequence_number_and_return(
+                    ResponseInProgressEvent(
+                        type="response.in_progress",
+                        sequence_number=-1,
+                        response=initial_response,
+                    )
                 )
             )
 
@@ -1903,12 +1924,15 @@ class OpenAIServingResponses(OpenAIServing):
                     created_time,
                     _increment_sequence_number_and_return,
                 ):
-                    yield event_data
+                    yield _emit(event_data)
             except GenerationError as e:
                 error_json = self._convert_generation_error_to_streaming_response(e)
-                yield _increment_sequence_number_and_return(
-                    TypeAdapter(StreamingResponsesResponse).validate_json(error_json)
+                yield _emit(
+                    _increment_sequence_number_and_return(
+                        TypeAdapter(StreamingResponsesResponse).validate_json(error_json)
+                    )
                 )
+                stream_end_hook()
                 return
 
             async def empty_async_generator():
@@ -1927,10 +1951,13 @@ class OpenAIServingResponses(OpenAIServing):
                 request_metadata,
                 created_time=created_time,
             )
-            yield _increment_sequence_number_and_return(
-                ResponseCompletedEvent(
-                    type="response.completed",
-                    sequence_number=-1,
-                    response=final_response,
+            yield _emit(
+                _increment_sequence_number_and_return(
+                    ResponseCompletedEvent(
+                        type="response.completed",
+                        sequence_number=-1,
+                        response=final_response,
+                    )
                 )
             )
+            stream_end_hook()
diff --git a/vllm/entrypoints/openai/speech_to_text/speech_to_text.py b/vllm/entrypoints/openai/speech_to_text/speech_to_text.py
index 31902bfa7..cf3772f4c 100644
--- a/vllm/entrypoints/openai/speech_to_text/speech_to_text.py
+++ b/vllm/entrypoints/openai/speech_to_text/speech_to_text.py
@@ -36,7 +36,12 @@ from vllm.entrypoints.openai.speech_to_text.protocol import (
     TranslationStreamResponse,
 )
 from vllm.entrypoints.openai.speech_to_text.utils import load_audio_bytes
-from vllm.entrypoints.utils import get_max_tokens
+from vllm.entrypoints.utils import (
+    get_max_tokens,
+    stream_emit_hook,
+    stream_end_hook,
+    stream_start_hook,
+)
 from vllm.exceptions import VLLMValidationError
 from vllm.inputs import EncoderDecoderInputs, ProcessorInputs
 from vllm.logger import init_logger
@@ -596,6 +601,7 @@ class OpenAISpeechToText(OpenAIServing):
         )
 
         try:
+            stream_start_hook()
             for result_generator in list_result_generator:
                 async for res in result_generator:
                     # On first result.
@@ -649,6 +655,7 @@ class OpenAISpeechToText(OpenAIServing):
                         )
 
                     data = chunk.model_dump_json(exclude_unset=True)
+                    stream_emit_hook()
                     yield f"data: {data}\n\n"
 
             # Once the final token is handled, if stream_options.include_usage
@@ -671,6 +678,7 @@ class OpenAISpeechToText(OpenAIServing):
                 final_usage_data = final_usage_chunk.model_dump_json(
                     exclude_unset=True, exclude_none=True
                 )
+                stream_emit_hook()
                 yield f"data: {final_usage_data}\n\n"
 
             # report to FastAPI middleware aggregate usage across all choices
@@ -683,6 +691,9 @@ class OpenAISpeechToText(OpenAIServing):
         except Exception as e:
             logger.exception("Error in %s stream generator.", self.task_type)
             data = self.create_streaming_error_response(e)
+            stream_emit_hook()
             yield f"data: {data}\n\n"
         # Send the final done message after all response.n are finished
+        stream_end_hook()
+        stream_emit_hook()
         yield "data: [DONE]\n\n"
diff --git a/vllm/entrypoints/utils.py b/vllm/entrypoints/utils.py
index 9550a41bb..fa5e7a43e 100644
--- a/vllm/entrypoints/utils.py
+++ b/vllm/entrypoints/utils.py
@@ -9,8 +9,13 @@ from argparse import Namespace
 from http import HTTPStatus
 from logging import Logger
 from string import Template
+from typing import Final
 
 import regex as re
+try:
+    import torch
+except ImportError:  # pragma: no cover
+    torch = None
 from fastapi import Request
 from fastapi.responses import JSONResponse, StreamingResponse
 from starlette.background import BackgroundTask, BackgroundTasks
@@ -30,6 +35,11 @@ from vllm.utils.argparse_utils import FlexibleArgumentParser
 
 logger = init_logger(__name__)
 
+_STREAM_END_OP_NAMESPACE: Final[str] = "_C"
+_STREAM_START_OP_NAME: Final[str] = "stream_start_hook"
+_STREAM_EMIT_OP_NAME: Final[str] = "stream_emit_hook"
+_STREAM_END_OP_NAME: Final[str] = "stream_end_hook"
+
 VLLM_SUBCMD_PARSER_EPILOG = (
     "For full list:            vllm {subcmd} --help=all\n"
     "For a section:            vllm {subcmd} --help=ModelConfig    (case-insensitive)\n"  # noqa: E501
@@ -38,6 +48,45 @@ VLLM_SUBCMD_PARSER_EPILOG = (
 )
 
 
+def stream_end_hook() -> None:
+    """Invoke the optional stream end hook if it is available."""
+    if torch is None:
+        return
+
+    if not hasattr(torch.ops, _STREAM_END_OP_NAMESPACE):
+        return
+
+    op_namespace = getattr(torch.ops, _STREAM_END_OP_NAMESPACE)
+    if hasattr(op_namespace, _STREAM_END_OP_NAME):
+        getattr(op_namespace, _STREAM_END_OP_NAME)(torch.empty(0))
+
+
+def stream_start_hook() -> None:
+    """Invoke the optional stream start hook if it is available."""
+    if torch is None:
+        return
+
+    if not hasattr(torch.ops, _STREAM_END_OP_NAMESPACE):
+        return
+
+    op_namespace = getattr(torch.ops, _STREAM_END_OP_NAMESPACE)
+    if hasattr(op_namespace, _STREAM_START_OP_NAME):
+        getattr(op_namespace, _STREAM_START_OP_NAME)(torch.empty(0))
+
+
+def stream_emit_hook() -> None:
+    """Invoke the optional stream emit hook if it is available."""
+    if torch is None:
+        return
+
+    if not hasattr(torch.ops, _STREAM_END_OP_NAMESPACE):
+        return
+
+    op_namespace = getattr(torch.ops, _STREAM_END_OP_NAMESPACE)
+    if hasattr(op_namespace, _STREAM_EMIT_OP_NAME):
+        getattr(op_namespace, _STREAM_EMIT_OP_NAME)(torch.empty(0))
+
+
 async def listen_for_disconnect(request: Request) -> None:
     """Returns if a disconnect message is received"""
     while True:
